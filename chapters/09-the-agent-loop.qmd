# The Agent Loop

*This chapter will cover:*

- The canonical pattern: `repeat ... until done.called()`
- Multi-turn conversations
- Iteration limits for safety
- Logging with `Log.info()`, `Log.warn()`
- Handling agent responses
- Per-turn tool control
