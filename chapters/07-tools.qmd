# Tools

*This chapter will cover:*

- Why tools matter: giving agents capabilities
- Inline tool definitions (`search = Tool { ... }`)
- The parameters/input schema
- Writing tool handler functions
- Tool return values
- Tool tracking: `done.called()`, `done.last_call()`, `done.last_result()`
